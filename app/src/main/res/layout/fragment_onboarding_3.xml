<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <!-- Белая карточка с закругленными углами -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="48dp"
        app:cardBackgroundColor="@color/onboarding_card"
        app:cardCornerRadius="24dp"
        app:cardElevation="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="32dp">

            <!-- Иллюстрация онбординга 3 - Успех и достижения -->
            <ImageView
                android:id="@+id/ivIllustration"
                android:layout_width="280dp"
                android:layout_height="280dp"
                android:layout_marginBottom="32dp"
                android:scaleType="fitCenter"
                android:contentDescription="Достижения и успех"
                android:src="@drawable/onboarding_3_success"
                tools:ignore="ContentDescription" />

            <!-- Заголовок -->
            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                android:gravity="center"
                android:text="@string/onboarding_3_title"
                android:textColor="@color/onboarding_text_primary"
                android:textSize="24sp"
                android:textStyle="bold"
                tools:text="Добивайся большего каждый день!" />

            <!-- Описание -->
            <TextView
                android:id="@+id/tvDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:lineSpacingExtra="4dp"
                android:text="@string/onboarding_3_description"
                android:textColor="@color/onboarding_text_secondary"
                android:textSize="16sp"
                tools:text="Завершай задачи, строй полезные привычки и возвращай себе контроль над своим временем" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>

