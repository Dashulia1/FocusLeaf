# 🚀 Инструкция по загрузке проекта на GitHub

## Шаг 1: Проверка Git конфигурации

Перед началом убедитесь, что Git настроен:

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"
```

Если уже настроено, пропустите этот шаг.

## Шаг 2: Инициализация репозитория (если еще не сделано)

```bash
cd /Users/dasasergeeva/AndroidStudioProjects/FocusLeaf2
git init
```

## Шаг 3: Добавление файлов

```bash
# Добавить все файлы (кроме тех, что в .gitignore)
git add .

# Проверить, что будет добавлено
git status
```

## Шаг 4: Первый commit

```bash
git commit -m "Initial commit: FocusLeaf 2.0 - Pomodoro productivity app"
```

## Шаг 5: Создание репозитория на GitHub

1. Откройте [GitHub.com](https://github.com)
2. Войдите в свой аккаунт
3. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
4. Заполните форму:
   - **Repository name**: `FocusLeaf2` (или любое другое имя)
   - **Description**: "Приложение для повышения продуктивности на основе техники Pomodoro"
   - Выберите **Public** или **Private**
   - **НЕ** добавляйте README, .gitignore или license (они уже есть)
5. Нажмите **"Create repository"**

## Шаг 6: Подключение к GitHub репозиторию

После создания репозитория GitHub покажет инструкции. Выполните команды:

```bash
# Замените YOUR_USERNAME на ваш GitHub username
# Замените FocusLeaf2 на имя репозитория, если выбрали другое

git remote add origin https://github.com/YOUR_USERNAME/FocusLeaf2.git

# Или используйте SSH (если настроен):
# git remote add origin git@github.com:YOUR_USERNAME/FocusLeaf2.git
```

## Шаг 7: Загрузка кода на GitHub

```bash
# Переименовать ветку на main (если нужно)
git branch -M main

# Загрузить код
git push -u origin main
```

Если потребуется авторизация:
- Для HTTPS: используйте Personal Access Token (не пароль)
- Для SSH: убедитесь, что SSH ключи настроены

## Быстрая команда (все шаги сразу)

После создания репозитория на GitHub:

```bash
cd /Users/dasasergeeva/AndroidStudioProjects/FocusLeaf2
git init
git add .
git commit -m "Initial commit: FocusLeaf 2.0 - Pomodoro productivity app"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/FocusLeaf2.git
git push -u origin main
```

## Настройка Personal Access Token (для HTTPS)

Если используете HTTPS и требуются учетные данные:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен
5. При запросе пароля используйте этот токен

## Проверка статуса

```bash
# Проверить статус
git status

# Проверить подключенные remote репозитории
git remote -v

# Проверить коммиты
git log --oneline
```

## Следующие коммиты

После первого коммита, для дальнейших изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

---

**Готово!** 🎉 Ваш проект теперь на GitHub!

