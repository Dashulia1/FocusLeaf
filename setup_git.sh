#!/bin/bash

# Скрипт для инициализации Git репозитория и подготовки к загрузке на GitHub

echo "🚀 Настройка Git репозитория для FocusLeaf2..."

# Переход в директорию проекта
cd "$(dirname "$0")"

# Проверка, инициализирован ли уже репозиторий
if [ -d ".git" ]; then
    echo "⚠️  Git репозиторий уже инициализирован"
else
    echo "📦 Инициализация Git репозитория..."
    git init
fi

# Проверка конфигурации Git
if [ -z "$(git config user.name)" ]; then
    echo "⚠️  Git user.name не настроен"
    echo "Выполните: git config --global user.name 'Ваше Имя'"
fi

if [ -z "$(git config user.email)" ]; then
    echo "⚠️  Git user.email не настроен"
    echo "Выполните: git config --global user.email 'ваш.email@example.com'"
fi

# Добавление файлов
echo "📝 Добавление файлов в репозиторий..."
git add .

# Проверка статуса
echo ""
echo "📊 Статус репозитория:"
git status --short | head -20

echo ""
echo "✅ Файлы подготовлены для коммита!"
echo ""
echo "Следующие шаги:"
echo "1. Проверьте статус: git status"
echo "2. Создайте коммит: git commit -m 'Initial commit: FocusLeaf 2.0'"
echo "3. Создайте репозиторий на GitHub.com"
echo "4. Подключите remote: git remote add origin https://github.com/USERNAME/FocusLeaf2.git"
echo "5. Загрузите код: git push -u origin main"
echo ""
echo "Подробная инструкция в файле GITHUB_SETUP.md"

